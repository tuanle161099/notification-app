export type NotificationData = {
  title: string
  thumbnail: string
  description: string
  createdAt: string
}

export type NotificationPayload = {
  title: string
  thumbnail: string
  description: string
  topic_name: string
}
